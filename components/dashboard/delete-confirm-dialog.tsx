'use client'

import { AlertTriangle, Trash2, X } from 'lucide-react'

export function DeleteConfirmDialog({ petName, onCancel, onConfirm }: { petName: string; onCancel: () => void; onConfirm: () => void }) {
  return <div className="fixed inset-0 z-[70] grid place-items-center bg-foreground/30 p-4 backdrop-blur-sm"><div role="dialog" aria-modal="true" aria-labelledby="delete-dialog-title" className="w-full max-w-md rounded-3xl border border-foreground/10 bg-card p-6 shadow-2xl sm:p-7"><div className="flex items-start justify-between gap-4"><span className="grid size-12 place-items-center rounded-2xl bg-rose-100 text-rose-600"><AlertTriangle className="size-6" /></span><button type="button" onClick={onCancel} aria-label="Cerrar" className="rounded-xl p-2 text-muted-foreground transition hover:bg-secondary hover:text-foreground"><X className="size-5" /></button></div><h2 id="delete-dialog-title" className="mt-5 text-xl font-semibold">¿Eliminar a {petName}?</h2><p className="mt-2 text-sm leading-6 text-muted-foreground">Se eliminará su perfil, fotos y datos del catálogo. Esta acción no se puede deshacer.</p><div className="mt-7 flex flex-col-reverse gap-3 sm:flex-row sm:justify-end"><button type="button" onClick={onCancel} className="rounded-full border border-foreground/15 px-5 py-2.5 text-sm font-medium transition hover:bg-secondary">Conservar perfil</button><button type="button" onClick={onConfirm} className="inline-flex items-center justify-center gap-2 rounded-full bg-rose-600 px-5 py-2.5 text-sm font-semibold text-white transition hover:bg-rose-700"><Trash2 className="size-4" /> Eliminar definitivamente</button></div></div></div>
}
